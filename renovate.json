{
    "enabledManagers": ["custom.regex"],
    "customManagers": [
        {
            "fileMatch": [
                "^Dockerfile$"
            ],
            "customType": "regex",
            "datasourceTemplate": "github-tags",
            "depNameTemplate": "konflux-ci/pipeline-migration-tool",
            "matchStrings": [
                "ARG PIPELINE_MIGRATION_TOOL_VERSION=v(?<currentValue>[\\d\\.]+)"
            ],
            "versioningTemplate": "semver"
        }
    ]
}
